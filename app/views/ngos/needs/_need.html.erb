<dl class="dl-horizontal">
  <dt><%= Need.human_attribute_name :user_id %></dt>
  <dd><%= need.user.organization %></dd>
  <dt><%= Need.human_attribute_name :volunteers_needed %></dt>
  <dd><%= need.volunteerings_count %> / <%= need.volunteers_needed %></dd>
  <dt><%= Need.human_attribute_name :category %></dt>
  <dd><%= need.i18n_category %></dd>
  <dt><%= Need.human_attribute_name :description %></dt>
  <dd><%= need.description %></dd>
  <dt><%= Need.human_attribute_name :city %></dt>
  <dd><%= need.city %></dd>
  <dt><%= Need.human_attribute_name :location %></dt>
  <dd><%= need.location %></dd>
  <dt><%= Need.human_attribute_name :start_time %> <%= Need.human_attribute_name :end_time %></dt>
  <dd><%= l need.start_time, format: '%d.%m.%y %H:%M' %> - <%= l need.end_time, format: '%d.%m.%y %H:%M' %></dd>
</dl>
<div class="row">
  <div class="col-sm-10 col-sm-offset-1 col-xs-12">
    <%= link_to t('helpers.links.edit'),
                edit_ngos_need_path(need), class: 'btn btn-default' %>
    <%= link_to t('helpers.links.destroy'),
                ngos_need_path(need),
                method: :delete,
                data: { confirm: t('helpers.links.confirm') },
                class: 'btn btn-danger' %>
    <br><br>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-hover table-clickable">
    <caption><%= Volunteering.model_name.human(count: 2) %></caption>
    <thead>
      <tr>
        <th><%= User.human_attribute_name(:id) %></th>
        <th><%= User.human_attribute_name(:first_name) %></th>
        <th><%= User.human_attribute_name(:last_name) %></th>
        <th><%= User.human_attribute_name(:email) %></th>
        <th><%= User.human_attribute_name(:phone) %></th>
        <th><%= Volunteering.human_attribute_name(:created_at) %></th>
      </tr>
    </thead>
    <tbody>
      <% need.volunteerings.each do |volunteering| %>
        <tr>
          <td><%= volunteering.user.id %></td>
          <td><%= volunteering.user.first_name %></td>
          <td><%= volunteering.user.last_name %></td>
          <td><%= volunteering.user.email %></td>
          <td><%= volunteering.user.phone %></td>
          <td><%= l volunteering.created_at, format: '%d.%m. %H:%M' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
