<% if params[:filter].present? %>
  $('div#list-wrapper').html("<%= j render 'list', needs: @needs %>");
<% else %>
  $('div.list-item').last().after("<%= j render @needs %>");
  $('a.view-more').replaceWith("<%= j view_more_link @needs %>")
<% end %>
