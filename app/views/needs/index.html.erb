<%= render 'filter_form' %>
<div id="needs">
  <% if @needs %>
    <%= render(@needs) %>
  <% else %>
    <%= render('shared/sorry_message', message: t('.empty')) %>
  <% end %>
</div>
<div class="row">
  <div class="col-sm-offset-4 col-sm-4 text-center">
    <%= view_more_link(@needs) %>
  </div>
</div>
