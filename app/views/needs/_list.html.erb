<% if needs.empty? %>
  <%= render 'shared/sorry_message', message: t('.sorry') %>
<% else %>
  <%= render needs %>
<% end %>

<div class="row">
  <div class="col-sm-offset-4 col-sm-4 text-center">
    <%= view_more_link needs %>
  </div>
</div>
